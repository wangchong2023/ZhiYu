# .env.example
# ZhiYu 项目本地环境变量模板
#
# 使用方式：
#   cp .env.example .env.local
#   编辑 .env.local，填写你本地的路径
#   在 shell profile (~/.zshrc 或 ~/.bash_profile) 中 source .env.local
#   或使用 direnv: https://direnv.net/
#
# 注意：.env.local 已加入 .gitignore，不会提交到仓库
#

# 开源库本地克隆根目录（包含所有 opensrc 开源库的父目录）
# 所有开源库均应克隆到此目录下，命名与 project.yml 中保持一致
# 示例：/Users/yourname/Documents/work/code/opensrc/swift
OPENSRC_ROOT=/path/to/your/opensrc/swift

# XcodeGen 生成项目时需要此变量已导出到环境
# 验证方式：echo $OPENSRC_ROOT
