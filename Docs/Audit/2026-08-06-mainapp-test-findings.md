# 主 App 测试驱动问题发现记录

> **起始日期**：2026-08-06
> **关联规格**：`Docs/superpowers/specs/2026-08-06-mainapp-coverage-design.md`
> **关联计划**：`docs/superpowers/plans/2026-08-06-mainapp-coverage.md`

## 严重程度定义

- 🔴 **P0**：数据损坏/丢失、崩溃、安全漏洞、资金损失（订阅/支付）
- 🟡 **P1**：逻辑错误、行为不一致、边界处理缺陷、错误处理缺失
- 🟢 **P2**：健壮性不足、维护负担、死代码、命名/注释问题

## 问题清单

| 序号 | 严重程度 | 文件:行号 | 问题类型 | 问题描述 | 黑盒影响 | 修改方案 | 处理状态 | 批次 |
|------|---------|----------|---------|---------|---------|---------|---------|------|
| 1 | 🟢 P2 | `Sources/Core/Base/Constants/LogAction.swift:23,35-36,40-41` | 契约不一致 | `export="action.export"`、`aiscanFailed/aiscanSkipped="log.action.aiscan.*"`、`error="ERROR"`、`unknown="unknown"` 不符合 `logAction.*` 前缀契约 | 日志分析时按 `logAction.` 前缀过滤会遗漏这些动作 | 统一改为 `logAction.export`/`logAction.aiscan.failed`/`logAction.aiscan.skipped`/`logAction.error`/`logAction.unknown` | 已修复 | 批次1 |
| 2 | 🟡 P1 | `Sources/Localization/Catalogs/Ingest.xcstrings` (`error.internalError`) | 本地化缺陷 | `error.internalError` 的值是固定 `"内部错误"`，无 `%@` 格式化占位符，导致 `ExportError.internalError(msg)` 的关联值 `msg` 被丢弃 | 用户看到"内部错误"但无法知道具体错误详情，不利于排查问题 | 在 `error.internalError` 的各语言值中添加 `%@` 占位符（如 `"内部错误：%@"`），使 `trf` 能正确注入关联值 | 已修复 | 批次1 |
| 3 | 🔴 P0 | `Sources/Core/Base/Utils/ZipUtility.swift:35,44-46,61` | 崩溃（对齐违规） | `UnsafeRawPointer.load(fromByteOffset:as:)` 用于读取 ZIP 头的 UInt16/UInt32 字段（偏移 8/18/28/30 均非对齐），在 ARM64 上触发 `EXC_BREAKPOINT` 崩溃 | 任何调用 `readZipArchive` 解析真实 ZIP 文件的场景都会崩溃（如导入 ZIP 备份） | 改用 `loadUnaligned(fromByteOffset:as:)`（iOS 15+） | 已修复 | 批次1 |
| 9 | 🔴 P0 | `Sources/Core/Base/Utils/ZipUtility.swift:44-45` | 偏移错误（ZIP 头解析） | `fileNameLength` 读取偏移 28（实际是 extra field length 位置），`extraFieldLength` 读取偏移 30（越界到文件名区域）。ZIP 规范中 file name length 在偏移 26，extra field length 在偏移 28 | 解析任何 ZIP 都会得到错误的文件名长度（0 或 extra field 长度），导致文件名解析失败、数据偏移错误，最终返回 nil | 修正偏移：fileNameLength 用 26，extraFieldLength 用 28 | 已修复 | 批次1 |
| 4 | 🟢 P2 | `Sources/Core/System/Security/JailbreakDetector.swift:22` | 可测试性 | `private init` 阻止测试构造独立实例，只能用 shared 单例无法隔离状态 | 测试无法独立构造实例验证 init 逻辑 | `private init` → `init`（internal） | 已修复 | 批次1 |
| 5 | 🟡 P1 | `Sources/Core/System/Security/JailbreakDetector.swift:50-61` | 逻辑错误（模拟器误报） | `checkCommonJailbreakFiles()` 检测 `/bin/bash`、`/usr/sbin/sshd`、`/usr/bin/ssh` 等路径，但这些在 macOS 系统自带存在。iOS 模拟器运行在 macOS 上，导致 `isJailbroken()` 在模拟器上永远返回 true | 越狱检测在模拟器/开发环境下完全失效，开发者无法信任检测结果；可能影响依赖此检测的安全逻辑 | 移除 macOS 自带路径（`/bin/bash`、`/usr/sbin/sshd`、`/usr/bin/ssh`），只保留 iOS 专属越狱路径 | 已修复 | 批次1 |
| 6 | 🟢 P2 | `Sources/Core/System/Analytics/LocalAnalyticsService.swift:20` | 可测试性 | `private init` + 硬编码 logURL 阻止测试注入临时路径 | 测试写入真实日志路径，污染用户数据 | `private init` → `init(logURL:)` 可注入 | 已修复 | 批次1 |
| 7 | 🟢 P2 | `Sources/Core/System/Workflow/WorkflowService.swift:17,20` | 可测试性 | `private init` + `@Inject` 阻止测试注入 Mock 依赖 | 测试只能用 shared 单例，无法隔离 ReminderService | `private init` → `init(reminderService:)` 可注入 | 已修复 | 批次1 |
| 8 | 🟡 P1 | `Sources/Core/System/Workflow/WorkflowService.swift:46` | 逻辑缺陷 | 任务过滤用 `line.hasPrefix("-")` 会匹配 `- [x]`（已完成任务），导致已完成任务也被同步 | 已完成事项被重复同步到系统提醒，造成冗余提醒 | 在过滤条件中排除 `hasPrefix(CoreConstants.MarkdownSyntax.taskDone)` 和 `taskDoneUpper` | 已修复 | 批次1 |
| 10 | 🟡 P1 | `Sources/Infrastructure/Processors/Graph/GraphCommunityProcessor.swift:252-310` | 算法缺陷 | Louvain 社区检测在全连通三角形图（3 节点 3 边）上无法合并社区，每个节点保持独立社区。`calculateModularityGain` 公式只计算"加入目标社区的增益"，未减去"离开当前社区的损失"，导致净增益计算错误，`bestGain` 始终为 0 | 全连通的知识图谱节点无法被识别为同一社区，社区发现功能在小规模全连通子图上失效 | 修正为标准 Louvain 净增益公式：`ΔQ = gainTarget - lossCurrent`，同时计算 `k_i_in(C_target)` 和 `k_i_in(C_current)`，以及 `Σ_tot(C_target)` 和 `Σ_tot(C_current)`（均排除节点 i 自身） | 已修复 | 批次2 |
| 11 | 🟡 P1 | `Sources/Infrastructure/LLM/OnDeviceLLMService.swift:417-438` | L10n 红线违规 | `OnDeviceError.errorDescription` 使用硬编码字符串（`"Model_not_found"`、`"Model_not_loaded"`、`"Not_supported"`、`"Inference_failed"`、`"Compilation_failed"`），违反 L10n 强约束红线（禁止硬编码文本，必须通过 `L10n.模块.属性` 强类型访问） | 端侧 LLM 错误信息无法本地化，所有语言用户看到相同的英文下划线文本 | 在 `L10n+AI.swift` 的 `OnDevice.Error` 枚举补充 `modelNotFound`/`modelNotLoaded`/`notSupported` 属性，`errorDescription` 改用 `L10n.AI.OnDevice.Error.*`（`inferenceFailed(String)` 用 `"\(L10n...): \(msg)"` 拼接） | 已修复 | 批次2 |
| 12 | 🟡 P1 | `Sources/Localization/Extensions/L10n+Watch.swift:35` | 本地化 key 缺失 | `widgetCapture` 属性引用 key `watch.widget.title`，该 key 在 `Platform.xcstrings` 中不存在，运行时返回 `[MISSING: watch.widget.title@Platform]` | watchOS Widget 标题显示为 `[MISSING: watch.widget.title@Platform]`，用户看到错误占位符而非本地化标题 | 改用已存在的语义匹配 key `watch.widget.displayName`（"ZhiYu Capture"/"智宇采集"） | 已修复 | 批次5 |
| 13 | 🟡 P1 | `Sources/Features/Knowledge/NotebookHub/Model/NotebookThemeFactory.swift:16-45` | 非确定性 bug | `semanticPalettes` 是 `Dictionary<String, [String]>`，`for...where` 遍历顺序不确定（Swift Dictionary hash 种子随机化），导致多关键词匹配时"first match wins"语义无法保证。同一输入 "tech art" 不同运行可能返回 tech 配色或 art 配色 | 笔记本主题配色非确定性，同一笔记本名称在不同启动时可能显示不同配色，用户体验不一致 | 将 `semanticPalettes` 从 `Dictionary` 改为有序数组 `[(keyword: String, colors: [String])]`，保证遍历顺序确定 | 已修复 | 批次5 |
| 14 | 🟡 P1 | `Sources/Infrastructure/Storage/Repositories/RAGGovernanceSQLiteStore.swift:33-55,60-86` | 时序边界 bug | `fetchTokenStats(days: 0)` 和 `fetchDailyAIStats(days: 0)` 的 `dateThreshold = Date()`（查询时刻），而记录的 `createdAt` 是插入时刻（早于查询时刻），导致 `createdAt >= dateThreshold` 为 false，`days=0` 永远返回空数据 | Token 统计的"今天"视图永远显示 0，用户无法查看当天的 Token 用量 | `days<=0` 时 `dateThreshold` 改用 `Calendar.current.startOfDay(for: Date())`（今天 0 点），确保当天所有记录被包含 | 已修复 | 批次5 |
| 15 | 🟢 P2 | `Tests/Unit/Core/OnboardingServiceTests.swift:17-25` | 测试隔离缺陷 | `OnboardingService.init()` 从共享 `UserDefaultsKeyStore` 读取 `hasCompletedOnboarding` 持久化值。前序测试 `testFinish_设置hasCompletedOnboarding为true` 调用 `finish()` 写入 `true` 到 UserDefaults，后续测试 `testInit_DI未就绪_hasCompletedOnboarding默认false` 新建 service 时读到持久化的 `true`，`XCTAssertFalse` 失败。测试名假设"DI未就绪"实际不成立——DI 已被前序测试填充，KeyStore 是真实的 `UserDefaultsKeyStore.shared`（非 Mock） | 测试间状态泄漏，`testInit_DI未就绪` 在全量测试中失败（单独运行通过，因 UserDefaults 干净） | 在 `setUp`/`tearDown` 中 `UserDefaults.standard.removeObject(forKey: AppConstants.Keys.Storage.hasCompletedOnboarding)`，确保每个测试从干净状态开始 | 已修复 | 批次6-A |
| 16 | 🟡 P1 | `Sources/Core/Base/Utils/ZipUtility.swift:149` | 解压缓冲区不足 | `decompressDeflate` 用 `data.count * ZipFormat.deflateBufferMultiplier`（10 倍）作为解压缓冲区。对高压缩比数据（如重复字符串 800 字节压缩为 26 字节），`26 * 10 = 260 < 800`，导致 `compression_decode_buffer` 只写入 260 字节，解压结果被截断 | 解压含高压缩比文件的 ZIP 时数据不完整，导入的备份/归档文件内容损坏（静默截断，无错误抛出） | 改用循环解压或动态扩容缓冲区：先尝试 10 倍，若 `compression_decode_buffer` 返回值等于缓冲区大小则倍增重试，直到返回值 < 缓冲区大小或达到上限 | 已确认 → 已修复 | 批次6-B |
| 17 | 🔴 P0 | `Sources/Infrastructure/Storage/Repositories/DatabaseWriterProvider.swift:24-36`、`KnowledgePageRepository.swift:18-27`、`VectorDataRepository.swift:19-28`、`Sources/Infrastructure/Storage/Engine/SQLiteStore.swift:23-36` | 静默降级导致数据丢失 | 4 个数据访问点在 `DatabaseManager.shared.dbWriter` 为 nil 时静默创建空内存 `DatabaseQueue()` 并返回。`dbWriter` 为 nil 的场景：(1) 启动早期 `setup()` 未完成；(2) `switchDatabase` 中 line 255 `self.dbWriter = nil` 到 line 274 重新赋值的瞬态窗口（含多个 `await` 让出点）；(3) `reset()` 后；(4) `releaseDatabaseConnection()` 后。`SQLiteStore` 有 1s 重试但最终仍降级；`DatabaseWriterProvider` 协议、`KnowledgePageRepository`、`VectorDataRepository` 无重试直接降级。降级后所有读写写入临时内存库，进程结束或下次切换后数据丢失，**无任何错误提示** | 启动早期或 Vault 热切换瞬态窗口期的所有数据写入静默丢失：用户新建/编辑的知识页、标签、链接、向量、反馈、RAG 治理记录等全部消失。极端情况下用户可能丢失大量工作内容，且无任何日志或 UI 提示 | **方案 A（已修复）**：移除静默降级，4 个数据访问点改为抛出 `DatabaseError.notReady`，`dbWriter` 属性改为 `get async throws`。所有调用方（61 处 `let writer = await dbWriter`）改为 `try await dbWriter`。`SQLiteStore` 保留 1s 重试但超时后抛错而非降级。调用方（Repository CRUD）已是 `async throws`，错误向上传播由 UI 层处理 | 已修复 | 批次6-C |
| 18 | 🟡 P1 | `Sources/Infrastructure/Storage/Persistence/DatabaseManager.swift:32-44,243-251` | 事务排空竞态 | `activeTransactionsCount` 的 `increment/decrement` 在 `@MainActor` 上串行化，但实际写事务在后台线程执行。`switchDatabase` 排空检查 `while activeTransactionsCount > 0` 时，存在 TOCTOU 竞态：(1) 检查 count==0 通过 → 后台事务刚 increment → 关闭连接导致写失败/数据损坏；(2) decrement 在 closeWriter 之后执行 → count 变为负数保护失效。此外 `decrementActiveTransactions` 的 `if > 0` 保护在竞态下可能掩盖真实计数 | Vault 热切换时后台写事务可能被强制中断，导致 WAL 未完整写回、数据损坏或 SQLITE_BUSY 错误。排空超时后的强制关闭可能丢失未提交事务 | **方案 A（已修复）**：新增 `TransactionGatekeeper` actor 封装事务计数与排空逻辑。`acquire()` 在排空期间（`draining == true`）抛出 `DatabaseError.draining` 拒绝新事务；`drain()` 设置 `draining = true` 并等待所有活跃事务完成（`activeCount == 0`），用 `DrainWaiter` 包装 `CheckedContinuation` 确保只 resume 一次。`DatabaseManager.incrementActiveTransactions` 改为 `async throws`，`decrementActiveTransactions` 改为 `async`，委托给 `transactionGatekeeper`。`switchDatabase` 排空逻辑改为 `await transactionGatekeeper.drain()`。`IngestService.ingestRawContent` 改为 `async throws`，用 `try await manager.incrementActiveTransactions()` + `defer { Task { await manager.decrementActiveTransactions() } }` | 已修复 | 批次6-C |
| 19 | 🟢 P2 | `Tests/Unit/System/CollaborationModelsTests.swift:35-36` | 测试时间戳不稳定 | `testCollabUser_hashable` 用 `Date()` 创建两个 `CollabUser`，两次调用 `Date()` 时间戳可能不同（毫秒级差异），导致 hash 不同，`Set` 去重失败，`XCTAssertEqual(set.count, 1)` 失败 | 测试不稳定，全量运行时偶发失败（取决于两次 `Date()` 调用是否跨毫秒边界） | 改用固定时间 `Date(timeIntervalSince1970:)` 确保相同字段 | 已确认 → 已修复 | 批次6-B |
| 20 | 🟡 P1 | `Sources/Infrastructure/Processors/FrontmatterParser.swift:331` | YAML 数组对象无法解析 | `processTopLevelLine` 中 `trimmed.split(separator: ":", maxSplits: 1, omittingEmptySubsequences: true)` 用了 `omittingEmptySubsequences: true`，导致 `key:`（value 为空，用于标记数组开始）被 split 成 1 段而非 2 段，`guard parts.count == 2` 失败直接 return，`currentArrayKey` 永远不被设置。所有 YAML 数组对象（如 `subjects:\n  - id: s1\n    name: X`）都无法解析，`convertYamlToJson` 返回 `{}` | Frontmatter 中的 YAML 数组对象（对比页的 subjects/dimensions/matrix）全部解析失败，静默降级为空对象。用户创建的对比页 Frontmatter 数据丢失，无任何错误提示 | `omittingEmptySubsequences: false`，这样 `key:` 会 split 成 `["key", ""]`，`parts.count == 2` 通过，`valueStr` 为空 → 设置 `currentArrayKey` | 已修复 | 批次7-A |
| 21 | 🟡 P1 | `Sources/Domain/RAG/LLMUtils.swift:16-23` + `Sources/Infrastructure/LLM/LLMRetrievalService.swift:118` | Rerank 索引解析失败 | `LLMUtils.parseJSONArray` 用 `JSONDecoder().decode([String].self)` 只能解析字符串数组 `["1","0","2"]`，但 `rerankChunks` 期望 LLM 返回数字索引数组 `[1,0,2]` 并用 `.compactMap { Int($0) }` 转换。`parseJSONArray("[1,0,2]")` 解析失败返回 `[]`，`rankedIndices = []`，rerank 结果等于原顺序（无重排效果） | Rerank 功能静默失效：LLM 返回的排序索引被丢弃，chunks 保持原顺序。RAG 检索质量下降，最相关的块可能不在最前面，影响 AI 合成质量 | `parseJSONArray` 改为支持数字数组：先尝试 `[String]`，失败后尝试 `[Int]` 并转为 `[String]`；或 `rerankChunks` 改用独立的数字数组解析方法 | 已修复 | 批次7-A |
| 22 | 🟢 P2 | `Sources/Infrastructure/LLM/LLMRetrievalService.swift:40,155-158` | 空 content 未降级到原查询 | `rewriteQuery` line 40 `LLMUtils.extractContent(from: response) ?? query` 和 `generateHypotheticalDocument` line 155-158 类似逻辑。当 LLM 返回空字符串 `""`（非 nil）时，`?? query` 不触发，返回 `""`。但方法文档注释说"若失败则退化返回原始查询" | LLM 返回空响应时，rewriteQuery 返回空字符串（后续检索用空查询，召回质量差），generateHypotheticalDocument 返回空字符串（HyDE 策略失效）。与文档注释承诺的"退化返回原始查询"不一致 | 空 string 视为失败：`(LLMUtils.extractContent(from: response).flatMap { $0.isEmpty ? nil : $0 }) ?? query` | 已修复 | 批次7-A |
| 23 | 🟡 P1 | `Sources/Infrastructure/LLM/ChatRunner.swift:62,103,170` + `ChatLLMService.swift:43,83,130` | 魔法数字（时间字面量） | `--uitesting` 自愈分支中硬编码 `UInt64(0.5 * 1_000_000_000)`、`UInt64(1.5 * 1_000_000_000)`、`UInt64(0.15 * 1_000_000_000)` 等浮点字面量与 `1_000_000_000` 魔法数字混用，违反 AGENTS.md 红线 #4（禁止内联魔鬼数字）。`0.5`/`1.5`/`0.15` 秒延迟语义不透明，且 `1_000_000_000`（纳秒/秒）未抽取为常量 | 维护负担：延迟值散落 3 处文件 6 个位置，调整延迟需逐处修改；语义不透明，新开发者无法理解 `0.15` 代表"字间吐字延迟" | 抽取到 `LLMConstants.UITesting` 命名空间：`mockInitialDelaySeconds`/`mockStreamChunkDelaySeconds`/`mockNonStreamDelaySeconds` + `nanosecondsPerSecond`，统一引用 | 已修复 | 批次7-I |
| 24 | 🟡 P1 | `Sources/Infrastructure/LLM/ChatRunner.swift:63,107` + `ChatLLMService.swift:45,88` | L10n 红线违规（Unicode 转义硬编码） | `--uitesting` 分支返回的 Mock 文本用 `\u{8FD9}\u{662F}...` Unicode 转义硬编码中文字符串（"这是针对UI测试的非流式Mock大模型回复内容。"等），违反 AGENTS.md L10n 强约束红线（禁止硬编码中文文本，必须通过 `L10n.模块.属性` 强类型访问）。用 Unicode 转义规避了 `check-code-localization.py` 的硬编码中文检测 | Mock 文本无法本地化，所有语言用户在 UI 测试中看到相同的中文文本；且 Unicode 转义规避检测的做法可能被模仿，导致后续 L10n 红线检测失效 | 改为纯 ASCII Mock 文本（`LLMConstants.UITesting.mockNonStreamReply`/`mockRAGReply`），避免 L10n 约束且语义清晰 | 已修复 | 批次7-I |
| 25 | 🟡 P1 | `Sources/Infrastructure/LLM/ChatRunner.swift:238` + `ChatLLMService.swift:136` | 魔法字符串（Mock 流式 chunks） | `runMockChatStream` 和 `ChatLLMService.chatStream` 的 `--uitesting` 分支硬编码 18 个字符串 chunks 数组（`["\u{8FD9}\u{662F}", "\u{5728}", "UI", ...]`），内容重复且散落两处文件，违反 AGENTS.md 红线 #4（禁止硬编码字符串）。两处数组内容完全相同但独立维护，易产生不一致 | 维护负担：修改 Mock 流式内容需同步修改两处；且 Unicode 转义硬编码中文同样违反 L10n 红线（见 #24） | 抽取为共享常量 `LLMConstants.UITesting.mockStreamChunks: [String]`，两处引用同一源 | 已修复 | 批次7-I |
| 26 | 🟢 P2 | `Sources/Infrastructure/LLM/ChatRunner.swift:367-370` | 逻辑边界（buffer 截断） | `StreamDeanonymizer.process` 中，当未找到 `]` 且 `remaining.count > maxRawLength`（25）时，输出 `remaining.prefix(count - bufferSuffixLength)` 并缓存 `suffix(bufferSuffixLength)`（10）。但若 `remaining.count` 恰好等于 25，走 else 分支缓存全部 25 字符到 buffer，下一轮 buffer+chunk 可能很长才输出。边界 `count == maxRawLength` 的处理不对称：>25 截断，==25 全缓存 | 极端情况下 buffer 积累 25 字符（含不完整占位符），若后续 chunk 不及时到达，输出延迟略增。实际影响较小，因 25 字符内必含 `[` 起始 | 边界统一：`>= maxRawLength` 时也执行截断 | 已修复 | 批次7-I |
| 27 | 🟡 P1 | `Sources/Infrastructure/LLM/LLMChatService.swift:30-44` | 魔法字符串（lengthHint 硬编码） | `buildChatMessages` 中 `lengthHint = "\nKeep response within \(PromptConstants.TokenLimits.defaultMaxOutputTokens) characters."` 硬编码英文指令字符串，违反 L10n 强约束红线（禁止硬编码文本）。该指令会注入到 systemPrompt 中发送给 LLM，影响所有语言用户的回复长度控制 | LLM 回复长度控制指令无法本地化，中文用户可能因英文指令理解偏差导致回复长度不符合预期；且硬编码英文违反 L10n 红线 | 抽取为 `LLMConstants.PromptInstruction.lengthHint(_:)`，通过强类型常量访问 | 已修复 | 批次7-I |
| 28 | 🟢 P2 | `Sources/Infrastructure/LLM/LLMChatService.swift:76-78` | 可测试性（SendableBody 包装） | `SendableBody` struct 用 `@unchecked Sendable` 包装 `[String: Any]` 字典以绕过并发检查，但 `[String: Any]` 本身非 Sendable。这是 Swift 6 严格并发模式下的变通方案，但 `@unchecked Sendable` 掩盖了潜在的并发安全问题（若 dict 内含非 Sendable 引用类型） | 维护负担：未来若 request body 包含非 Sendable 类型，可能引入并发隐患而编译器不告警 | 维持现状，补充文档注释说明 `@unchecked` 的前提条件（body 仅含 Sendable 类型） | 已修复 | 批次7-I |
| 29 | 🟡 P1 | `Sources/Infrastructure/LLM/OnDeviceLLMService.swift:312` | L10n 红线违规（硬编码英文） | `chatOnDevice` 中 `let prompt = "\(context)\n\n\("Question"): \(query)"` 硬编码字符串 `"Question"`，违反 L10n 强约束红线。该字符串会作为 prompt 发送给端侧 LLM，影响 prompt 语义的一致性 | 端侧 LLM 的 prompt 标签无法本地化，可能影响非英文语境下的 prompt 效果；且违反 L10n 红线 | 抽取为 `LLMConstants.PromptInstruction.questionLabel` | 已修复 | 批次7-I |
| 30 | 🟢 P2 | `Sources/Infrastructure/LLM/OnDeviceLLMService.swift:52` | 魔法字符串（UserDefaults key） | `configKey = "zhiyu_ondevice_config"` 硬编码 UserDefaults key，违反 AGENTS.md 红线 #4（禁止硬编码字符串，UserDefaults key 必须抽取为强类型常量） | 维护负担：key 散落各处，重命名需逐处修改；且与其他 UserDefaults key 风格不一致（部分已用 `AppConstants.Keys.*`） | 抽取到 `LLMConstants.OnDeviceStorage.configKey` | 已修复 | 批次7-I |
| 31 | 🟢 P2 | `Sources/Infrastructure/LLM/OnDeviceLLMService.swift:89,107,119` | 魔法字符串（模型 ID） | `discoverModels` 中硬编码 `"bundled_zhiyu"`、`"apple_intelligence"`、`"downloaded_"` 前缀等字符串字面量，部分已有 `LLMConstants.ModelIDPrefix.downloaded/bundled` 常量但未一致使用（line 89 用字面量 `"bundled_zhiyu"`，line 107 用 `LLMConstants.ModelIDPrefix.downloaded`） | 维护负担：模型 ID 前缀风格不一致，`"bundled_zhiyu"` 和 `"apple_intelligence"` 未抽取为常量 | 统一抽取到 `LLMConstants.OnDeviceModelID.bundledZhiyu`/`appleIntelligence`，所有引用改用常量 | 已修复 | 批次7-I |
| 32 | 🟡 P1 | `Sources/Infrastructure/LLM/OnDeviceLLMService.swift:253` | 逻辑缺陷（token 计数） | `generate` 中 `let tokenCount = generated.split(separator: " ").count` 用空格分割估算 token 数。但中文、日文等无空格分隔的语言 token 数被严重低估（整段中文算作 1 token），导致 `inferenceSpeed` 指标失真 | 端侧 LLM 推理速率指标对非英文内容完全失真，用户看到的 tokens/秒 数值错误（中文内容速率被严重高估） | 改用 `generated.count / PromptConstants.TokenLimits.charactersPerToken` 估算，与 `AIAnalyticsService.recordRAGMetrics` 的估算方式一致 | 已修复 | 批次7-I |
| 33 | 🟢 P2 | `Sources/Infrastructure/LLM/OnDeviceLLMService.swift:407-413` | 魔法字符串（SF Symbol 名） | `OnDeviceModel.icon` 中硬编码 `"cube.box.fill"`、`"arrow.down.circle.fill"`、`"apple.logo"` 等 SF Symbol 名，违反 AGENTS.md 红线 #4（禁止硬编码字符串） | 维护负担：SF Symbol 名散落，重命名需逐处修改；且与 `DesignSystem.Icons.*` 风格不一致 | 抽取到 `LLMConstants.OnDeviceIcon.bundled`/`downloaded`/`system` | 已修复 | 批次7-I |
| 34 | 🟡 P1 | `Sources/Infrastructure/Plugins/JavaScriptPlugin.swift:250` | 魔法数字（maxResponseSize） | `private let maxResponseSize = 5 * 1024 * 1024` 硬编码 5MB 限制，违反 AGENTS.md 红线 #4（禁止内联魔鬼数字，复合表达式中的换算常量必须抽取）。`5`、`1024`、`1024` 三个数字均为魔法数字，且 `1024` 未抽取为 `BytesPerKB` | 维护负担：5MB 阈值语义不透明，且 `1024` 在复合表达式中未被「常量定义行」规则吞掉，需显式抽取 | 新建 `PluginConstants.swift`，抽取为 `PluginConstants.Sandbox.maxResponseSizeBytes = 5 * Int(SystemConstants.bytesPerKB) * Int(SystemConstants.bytesPerKB)` | 已修复 | 批次7-I |
| 35 | 🟢 P2 | `Sources/Infrastructure/Plugins/JavaScriptPlugin.swift:224,239,259,290` | 魔法数字（时间限制） | `JSContextGroupSetExecutionTimeLimit(group, 0.5, ...)` 中 `0.5` 秒硬编码 4 处，违反 AGENTS.md 红线 #4。JS 执行时间限制散落多处，调整需逐处修改 | 维护负担：JS 沙箱时间限制语义不透明，且 4 处独立维护易不一致 | 抽取到 `PluginConstants.Sandbox.jsExecutionTimeLimitSeconds: Double = 0.5`，4 处统一引用 | 已修复 | 批次7-I |
| 36 | 🟡 P1 | `Sources/Infrastructure/Plugins/PluginLoader.swift:169` | 魔法字符串（requiredLocales） | `validateReadmeFiles` 中 `let requiredLocales = ["en", "zh-Hans"]` 硬编码必需语言列表，违反 AGENTS.md L10n 强约束（语言代码应通过常量或 `L10n` 管理） | 维护负担：新增支持语言时需修改此数组，且硬编码语言代码散落 | 抽取到 `PluginConstants.Localization.requiredLocales: [String]` | 已修复 | 批次7-I |
| 37 | 🟢 P2 | `Sources/Infrastructure/Plugins/PluginLoader.swift:347-353` | 魔法字符串（manifest 默认值） | `loadPluginFromRawJS` 中 `PluginManifest(id: "local.\(displayName)", version: "1.0.0", author: "Local Developer", permissions: ["log", "writeContent"], ...)` 硬编码版本号、作者名、权限列表等字符串，违反 AGENTS.md 红线 #4 | 维护负担：默认值散落，调整需逐处修改 | 抽取到 `PluginConstants.DefaultManifest.*`（version/author/permissions/idPrefix/descriptionEn） | 已修复 | 批次7-I |
| 38 | 🟡 P1 | `Sources/Infrastructure/Network/ModelDownloadManager.swift:319` | 魔法数字（bufferSize） | `let bufferSize = 1024 * 1024` 硬编码 1MB 缓冲区大小，违反 AGENTS.md 红线 #4（复合表达式中的换算常量必须抽取，`1024` 不被「常量定义行」规则吞掉） | 维护负担：缓冲区大小语义不透明，且 `1024` 在复合表达式中需显式抽取 | 抽取为 `ModelDownloadManager.sha256BufferSize = Int(SystemConstants.bytesPerKB) * Int(SystemConstants.bytesPerKB)`（private static），添加 `import UFPCore` | 已修复 | 批次7-I |
| 39 | 🟢 P2 | `Sources/Infrastructure/Network/ModelDownloadManager.swift:248` | 字符串拼接（错误消息） | `await manager.updateState(for: modelId, to: .failed(error: "Sandbox storage" + " allocation failed:" + " \(error.localizedDescription)"))` 用 3 段字符串拼接，语义割裂且违反 L10n 红线（硬编码英文错误消息） | 维护负担：错误消息风格不一致（其他错误用单字符串），且硬编码英文无法本地化 | 抽取为 `ModelDownloadManager.sandboxAllocationFailedPrefix`（private static），改用单段插值 `"\(Self.sandboxAllocationFailedPrefix) \(error.localizedDescription)"` | 已修复 | 批次7-I |
| 40 | 🟡 P1 | `Sources/Infrastructure/Processors/Network/WebScraperProcessor.swift:104,112` | 魔法字符串（测试域名） | `MockScraperHandler` 中硬编码 `"invalid-host-domain-never-exist-112233.com"` 和 `ProcessorConstants.Module.paywallTestDomain` 混用。`paywallTestDomain` 已抽取为常量但 `invalid-host-domain-never-exist-112233.com` 仍硬编码 | 维护负担：测试域名散落，且 `112233` 为魔鬼数字 | 抽取到 `ProcessorConstants.WebScraper.invalidHostTestDomain`，统一管理 | 已修复 | 批次7-I |
| 41 | 🟢 P2 | `Sources/Infrastructure/Processors/Network/WebScraperProcessor.swift:262-338` | 正则注入风险（NSRegularExpression） | `DumbExtractorHandler.extractFromHTML` 和 `cleanHTMLTags` 用 `NSRegularExpression` 解析 HTML，正则模式如 `<article[^>]*>(.*?)</article>`、`<p[^>]*>(.*?)</p>` 硬编码为字符串字面量。对嵌套标签、畸形 HTML 解析不可靠，且正则模式未抽取为常量 | 维护负担：正则模式散落 5 处，调整需逐处修改；且正则解析 HTML 对复杂页面可能提取错误内容 | 正则模式抽取到 `ProcessorConstants.HTMLRegex.*`（article/paragraph/listItem/tableCell/header） | 已修复 | 批次7-I |
| 42 | 🟡 P1 | `Sources/Infrastructure/Processors/Network/WebScraperProcessor.swift:325-333` | HTML 实体解码不完整 | `cleanHTMLTags` 的 `entities` 字典只覆盖 7 种常见实体（`&quot;`/`&amp;`/`&lt;`/`&gt;`/`&nbsp;`/`&apos;`/`&#39;`），未覆盖 `&#34;`/`&#60;`/`&#62;`/`&hellip;`/`&mdash;`/`&ndash;`/`&trade;` 等常见实体。且硬编码实体字典违反 L10n/魔法字符串红线 | 网页抓取的文本中未解码的 HTML 实体保留为原始字符串（如 `&#34;` 显示为字面量），影响 Markdown 内容质量 | 扩展实体字典至 16 条，抽取到 `ProcessorConstants.HTMLEntity.decodeMap` | 已修复 | 批次7-I |
| 43 | 🟢 P2 | `Sources/Infrastructure/Processors/ImageExtractor.swift:58` | 魔法字符串（imageFolders） | `extractImagesFromOfficeFile` 中 `let imageFolders = ["word/media", "xl/media", "ppt/media"]` 硬编码 Office 文档图片目录路径，违反 AGENTS.md 红线 #4 | 维护负担：目录路径散落，新增 Office 格式需修改此数组 | 抽取到 `ProcessorConstants.OOXML.imageMediaFolders: [String]` | 已修复 | 批次7-I |
| 44 | 🟡 P1 | `Sources/Infrastructure/Storage/Services/MaintenanceService.swift:84,86,106,114,117` | L10n 红线违规（硬编码日志 key） | `generateInitialNotebooks` 和 `seedDefaultContent` 中 `activeLogger.addLog(..., details: "InitialNotebook_PageCountRefreshed", ...)`、`"InitialNotebook_PageCountRefreshed"`、`StorageConstants.LogDetails.seededDefaultContent` 等混用。部分 details 用 `StorageConstants.LogDetails.*` 常量，但 line 84 `"InitialNotebook_PageCountRefreshed"` 和 line 117 `StorageConstants.LogDetails.seedFailedPrefix + "\(error)"` 硬编码字符串 | 维护负担：日志 details 风格不一致，部分硬编码部分用常量；且硬编码字符串违反 L10n/魔法字符串红线 | 在 `StorageConstants.LogDetails` 新增 `initialNotebookPageCountRefreshed` 常量，line 84 改用常量引用 | 已修复 | 批次7-I |
| 45 | 🟢 P2 | `Sources/Infrastructure/Storage/Services/MaintenanceService.swift:97` | 魔法字符串（环境变量名） | `ProcessInfo.processInfo.environment["UITesting"] == StorageConstants.OperationStatus.true` 中 `"UITesting"` 硬编码环境变量名，违反 AGENTS.md 红线 #4（禁止硬编码字符串） | 维护负担：环境变量名散落，与其他 `--uitesting` 检测（`ProcessInfo.processInfo.arguments.contains("--uitesting")`）风格不一致 | 在 `StorageConstants` 新增 `Environment` 命名空间（`uitestingEnvKey`/`uitestingLaunchArg`），line 97 改用常量引用 | 已修复 | 批次7-I |

## 处理状态说明

- **待确认**：测试发现，待用户确认
- **已确认 → 已修复**：用户确认后已修复
- **已确认 → 预期行为不修**：用户确认是预期行为，不修
